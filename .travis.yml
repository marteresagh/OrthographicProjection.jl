language: julia

os:
  - linux
  - osx

branches:
  only:
  - master

julia:
  - 1.4

notifications:
  email: false

after_success:
 - julia -e 'using Pkg; Pkg.add("Documenter"); Pkg.add("Coverage");'
  - julia -e 'import OrthographicProjection; cd(dirname(dirname(pathof(OrthographicProjection)))); include(joinpath("docs", "make.jl"))'
  - julia -e 'using Coverage; import OrthographicProjection; cd(dirname(dirname(pathof(OrthographicProjection)))); Codecov.submit(process_folder())'

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master
